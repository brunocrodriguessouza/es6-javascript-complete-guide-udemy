Coding Exercise 12: Finding Submitted Users

Given an array of users, return 'true' if every user has submitted a request form. Assign the result to the
variable 'hasSubmitted'.

const users = [
  { id: 21, hasSubmitted: true },
  { id: 62, hasSubmitted: false },
  { id: 4, hasSubmitted: true }
];


var hasSubmitted = users.every(user => user.hasSubmitted)

// var hasSubmitted = users.every(function(user) {
//     return user.hasSubmitted === true;
// });
